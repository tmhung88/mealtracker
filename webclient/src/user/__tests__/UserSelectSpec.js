import React from "react";
import { shallow } from "enzyme";
import { UserSelect } from "../UserSelect";

describe("#UserSelect", () => {
    it("should fetch data on searching", async ()=>{
        const api = {
        
        }
        const wrapper = shallow(<UserSelect classes={{}} />)
    })
})